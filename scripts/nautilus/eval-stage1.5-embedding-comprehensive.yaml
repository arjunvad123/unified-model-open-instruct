# Embedding Benchmark: Stage 1.5 (Ours, 3B) vs Qwen3-Embedding-4B
# MTEB Retrieval: NFCorpus, SciFact, ArguAna, SCIDOCS, FiQA2018
#
# Deploy: kubectl apply -f eval-stage1.5-embedding-comprehensive.yaml -n svcl-self-improve
# Logs:   kubectl logs job/eval-emb-bench -n svcl-self-improve -f

apiVersion: batch/v1
kind: Job
metadata:
  name: eval-emb-bench
  namespace: svcl-self-improve
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never

      containers:
        - name: eval
          image: nvidia/cuda:12.4.1-cudnn-devel-ubuntu22.04

          command: ["/bin/bash", "-c"]
          args:
            - |
              set -e

              echo "==========================================================="
              echo "Embedding Benchmark"
              echo "Stage 1.5 (Ours, 3B) vs Qwen3-Embedding-4B"
              echo "==========================================================="
              nvidia-smi --query-gpu=index,name,memory.total --format=csv

              export DEBIAN_FRONTEND=noninteractive
              apt-get update && apt-get install -y --no-install-recommends \
                curl ca-certificates python3 python3-pip python3-venv git

              python3 -m venv /opt/venv
              source /opt/venv/bin/activate

              pip install --upgrade pip
              pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
              pip install "transformers>=4.51.0" "sentence-transformers>=2.7.0" accelerate peft mteb numpy tqdm

              python -c "import torch; print(f'PyTorch {torch.__version__}, CUDA: {torch.cuda.is_available()}')"

              mkdir -p /workspace/results
              cd /workspace

              cat > run_eval.py << 'PYTHON_SCRIPT'
              import json, os, gc
              from datetime import datetime
              import numpy as np
              import torch
              import torch.nn.functional as F
              from tqdm import tqdm
              import warnings
              warnings.filterwarnings("ignore")

              TASKS = ["NFCorpus", "SciFact", "ArguAna", "SCIDOCS", "FiQA2018"]

              # ============================================================
              # Our model: CausalLM + output_hidden_states + mean pooling
              # ============================================================
              class CausalLMEmbedding:
                  def __init__(self, model_path, device=None):
                      from transformers import AutoTokenizer, AutoModelForCausalLM
                      self.device = device or ("cuda" if torch.cuda.is_available() else "cpu")
                      print(f"  Loading: {model_path}")
                      self.tokenizer = AutoTokenizer.from_pretrained(model_path, trust_remote_code=True)
                      self.model = AutoModelForCausalLM.from_pretrained(
                          model_path, trust_remote_code=True, torch_dtype=torch.bfloat16
                      ).to(self.device).eval()
                      if not self.tokenizer.pad_token:
                          self.tokenizer.pad_token = self.tokenizer.eos_token
                      print(f"  Params: {sum(p.numel() for p in self.model.parameters())/1e9:.1f}B")

                  def encode(self, sentences, batch_size=32, **kwargs):
                      all_emb = []
                      for i in tqdm(range(0, len(sentences), batch_size), desc="Encoding", disable=len(sentences)<100):
                          batch = sentences[i:i+batch_size]
                          inputs = self.tokenizer(batch, padding=True, truncation=True, max_length=512, return_tensors="pt").to(self.device)
                          with torch.no_grad():
                              out = self.model(**inputs, output_hidden_states=True, use_cache=False)
                              h = out.hidden_states[-1].float()
                              m = inputs["attention_mask"].unsqueeze(-1).float()
                              emb = F.normalize((h * m).sum(1) / m.sum(1).clamp(min=1e-9), p=2, dim=1)
                              all_emb.append(emb.cpu())
                      return torch.cat(all_emb, dim=0).numpy()

              # ============================================================
              # Qwen3-Embedding-4B: AutoModel + last-token pool + instruction
              # ============================================================
              class Qwen3Embedding:
                  def __init__(self, device=None):
                      from transformers import AutoTokenizer, AutoModel
                      self.device = device or ("cuda" if torch.cuda.is_available() else "cpu")
                      name = "Qwen/Qwen3-Embedding-4B"
                      print(f"  Loading: {name}")
                      self.tokenizer = AutoTokenizer.from_pretrained(name, trust_remote_code=True, padding_side='left')
                      self.model = AutoModel.from_pretrained(
                          name, trust_remote_code=True, torch_dtype=torch.float16
                      ).to(self.device).eval()
                      self.instruction = "Given a web search query, retrieve relevant passages that answer the query"
                      print(f"  Params: {sum(p.numel() for p in self.model.parameters())/1e9:.1f}B")

                  def encode(self, sentences, batch_size=8, prompt_name=None, **kwargs):
                      all_emb = []
                      for i in tqdm(range(0, len(sentences), batch_size), desc="Encoding", disable=len(sentences)<100):
                          batch = sentences[i:i+batch_size]
                          if prompt_name == "query":
                              batch = [f"Instruct: {self.instruction}\nQuery:{s}" for s in batch]
                          inputs = self.tokenizer(batch, padding=True, truncation=True, max_length=512, return_tensors="pt")
                          inputs = {k: v.to(self.device) for k, v in inputs.items()}
                          with torch.no_grad():
                              out = self.model(**inputs)
                              seq_lens = inputs["attention_mask"].sum(dim=1) - 1
                              emb = out.last_hidden_state[torch.arange(len(batch), device=self.device), seq_lens].float()
                              emb = F.normalize(emb, p=2, dim=1)
                              all_emb.append(emb.cpu())
                      return torch.cat(all_emb, dim=0).numpy()

              # ============================================================
              # Evaluation
              # ============================================================
              def evaluate_task(model, task_name, use_query_prompt=False):
                  import mteb
                  try:
                      task = mteb.get_task(task_name)
                      task.load_data()
                      subset = list(task.dataset.keys())[0]
                      split = "test" if "test" in task.dataset[subset] else list(task.dataset[subset].keys())[0]
                      data = task.dataset[subset][split]

                      corpus = {r["id"]: {"title": r.get("title",""), "text": r.get("text","")} for r in data["corpus"]}
                      queries = {r["id"]: r["text"] for r in data["queries"]}
                      qrels = data["relevant_docs"]
                      print(f"    Corpus: {len(corpus)}, Queries: {len(queries)}")

                      corpus_ids = list(corpus.keys())
                      corpus_texts = [f"{corpus[c].get('title','')} {corpus[c].get('text','')}".strip() for c in corpus_ids]
                      corpus_emb = model.encode(corpus_texts, batch_size=32)

                      query_ids = list(queries.keys())
                      query_texts = [queries[q] for q in query_ids]
                      query_emb = model.encode(query_texts, batch_size=32, prompt_name="query" if use_query_prompt else None)

                      scores = np.dot(query_emb, corpus_emb.T)

                      h1, h10, mrr, ndcg, n = 0, 0, 0, 0, 0
                      for i, qid in enumerate(query_ids):
                          if qid not in qrels: continue
                          n += 1
                          rel = set(qrels[qid].keys())
                          ranked = np.argsort(scores[i])[::-1]
                          top10 = [corpus_ids[idx] for idx in ranked[:10]]
                          if any(d in rel for d in top10[:1]): h1 += 1
                          if any(d in rel for d in top10[:10]): h10 += 1
                          for rank, idx in enumerate(ranked):
                              if corpus_ids[idx] in rel:
                                  mrr += 1/(rank+1)
                                  break
                          dcg = sum(1/np.log2(r+2) for r, idx in enumerate(ranked[:10]) if corpus_ids[idx] in rel)
                          idcg = sum(1/np.log2(j+2) for j in range(min(len(rel), 10)))
                          if idcg > 0: ndcg += dcg/idcg

                      return {"hits@1": h1/n, "hits@10": h10/n, "ndcg@10": ndcg/n, "mrr": mrr/n}
                  except Exception as e:
                      print(f"    Error: {e}")
                      import traceback; traceback.print_exc()
                      return {"error": str(e)}

              def main():
                  MODELS = [
                      ("Stage 1.5 (Ours, 3B)", "stage1_5", lambda: CausalLMEmbedding("Arjunvad/unified-model-stage1-5-embedding-v2"), False),
                      ("Qwen3-Embedding-4B", "qwen3_emb_4b", lambda: Qwen3Embedding(), True),
                  ]

                  all_results = {}
                  for display_name, key, loader, use_prompt in MODELS:
                      print("\n" + "="*70)
                      print(f"EVALUATING: {display_name}")
                      print("="*70)
                      try:
                          model = loader()
                          results = {}
                          for task in TASKS:
                              print(f"\n  {task}...")
                              r = evaluate_task(model, task, use_prompt)
                              results[task] = r
                              if "error" not in r:
                                  print(f"    Hits@1={r['hits@1']:.4f}  NDCG@10={r['ndcg@10']:.4f}  MRR={r['mrr']:.4f}")
                          all_results[key] = results
                          del model; torch.cuda.empty_cache(); gc.collect()
                      except Exception as e:
                          print(f"  FAILED: {e}")
                          import traceback; traceback.print_exc()
                          all_results[key] = {"error": str(e)}

                  # Print comparison
                  keys = ["stage1_5", "qwen3_emb_4b"]
                  names = {"stage1_5": "Stage 1.5 (Ours)", "qwen3_emb_4b": "Qwen3-Emb 4B"}

                  print("\n" + "="*90)
                  print("EMBEDDING BENCHMARK RESULTS")
                  print("="*90)

                  for metric in ["hits@1", "hits@10", "ndcg@10", "mrr"]:
                      print(f"\n--- {metric.upper()} ---")
                      header = f"{'Task':<15}" + "".join(f"{names[k]:<25}" for k in keys)
                      print(header); print("-"*len(header))
                      for task in TASKS:
                          row = f"{task:<15}"
                          for k in keys:
                              if k in all_results and task in all_results[k] and "error" not in all_results[k].get(task,{}):
                                  row += f"{all_results[k][task].get(metric,0):<25.4f}"
                              else:
                                  row += f"{'N/A':<25}"
                          print(row)
                      print("-"*len(header))
                      row = f"{'AVERAGE':<15}"
                      for k in keys:
                          if k in all_results and "error" not in all_results.get(k,{}):
                              vals = [all_results[k][t].get(metric,0) for t in TASKS if t in all_results[k] and "error" not in all_results[k].get(t,{})]
                              row += f"{sum(vals)/len(vals) if vals else 0:<25.4f}"
                          else:
                              row += f"{'N/A':<25}"
                      print(row)

                  with open("results/embedding_benchmark.json","w") as f:
                      json.dump({"timestamp": datetime.now().isoformat(), "tasks": TASKS, "results": all_results}, f, indent=2)
                  print(f"\nSaved to results/embedding_benchmark.json")
                  print("="*90)

              if __name__ == "__main__":
                  main()
              PYTHON_SCRIPT

              python run_eval.py

              echo "Done. Pod alive 30min for log inspection..."
              sleep 1800

          resources:
            requests:
              memory: "32Gi"
              cpu: "4"
              nvidia.com/gpu: "1"
            limits:
              memory: "48Gi"
              cpu: "8"
              nvidia.com/gpu: "1"

          env:
            - name: HF_HOME
              value: "/tmp/hf_cache"
            - name: TRANSFORMERS_CACHE
              value: "/tmp/hf_cache"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: token

      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: PreferNoSchedule
